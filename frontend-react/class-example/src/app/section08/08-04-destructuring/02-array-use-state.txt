function useState(받는초기값변수) {

    let state변수 = 받는초기값변수

    const setState함수 = (변경값) => {
        console.log(`${state변수}값에서 ${변경값}으로 변경하겠습니다!`)
        console.log(`${변경값}을 착 달라붙게끔 화면을 다시 그리겠습니다(리렌더링)`)
    }
    return [state변수, setState함수]
}
undefined
const[count, setCount] = useState(0)
undefined
count
0
setCount(3)
VM1285:6 0값에서 3으로 변경하겠습니다!
VM1285:7 3을 착 달라붙게끔 화면을 다시 그리겠습니다(리렌더링)
undefined
const result = useState(0)
undefined
result[0]
0
result[1]
(변경값) => {
        console.log(`${state변수}값에서 ${변경값}으로 변경하겠습니다!`)
        console.log(`${변경값}을 착 달라붙게끔 화면을 다시 그리겠습니다(리렌더링)`)
    }
result[1](3)
VM1285:6 0값에서 3으로 변경하겠습니다!
VM1285:7 3을 착 달라붙게끔 화면을 다시 그리겠습니다(리렌더링)
undefined